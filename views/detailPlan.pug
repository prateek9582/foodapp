.signup-child
    .signup-header
        h3 #{plan.name}
        .signup-pricing
            span.pricing $#{plan.price}
            | /month
            p That’s only #{plan.price}$ per meal
    .signup-features
        ul
            li
                i.fas.fa-check.small-icon  
                | 1 meal every day
            li
                i.fas.fa-check.small-icon  
                | Duration #{plan.duration}
            li
                i.fas.fa-check.small-icon  
                | #{plan.description}
            li
                i.fas.fa-check.small-icon  
                | Free delivery
        .signup-button
            if user
                a.btn.btn-full(href=`javascript:void(0)` planId = `${plan["_id"]}` userId = `${user["_id"]}`).bookplan I'm hungry
            else
                a.btn.btn-full(href='/login') I'm hungry